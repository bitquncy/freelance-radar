Implemented the production-hardening audit pass across concurrency, security, runtime operations, migrations, and regression coverage.

Changed files:
- `.env.example`
- `.github/workflows/ci.yml`
- `alembic/versions/b7d8a42c9e10_connection_normalized_identity.py`
- `bot/handlers/v2/crm_handlers.py`
- `bot/handlers/v2/sources.py`
- `config.py`
- `core/billing.py`
- `core/models.py`
- `docker-compose.yml`
- `docs/PRODUCTION_OPERATIONS.md`
- `main.py`
- `monitoring/worker.py`
- `scripts/backup_db.py`
- `scripts/entrypoint.sh`
- `scripts/healthcheck.py`
- `scripts/purge_data.py`
- `scripts/restore_db.py`
- `tests/integration/test_postgres_smoke.py`

Implemented behavior:
- Subscription entitlement updates now lock the PostgreSQL user row, preventing lost extensions from distinct concurrent charges while retaining charge-ID idempotency.
- Payment record and entitlement update remain within the handler’s single transaction.
- Reminder claims use row locking with `SKIP LOCKED`, so only one worker claims a pending reminder.
- Telegram sources have normalized identities and a unique DB constraint.
- Channel quota allocation serializes on the owning user row.
- Migration preserves existing duplicate channels by pausing and uniquely marking later duplicates rather than deleting user data.
- Reminder callbacks reject mismatched `reminder_id`/`client_id` pairs.
- Telegram exception logging includes traceback and safe update/user/chat correlation metadata, without message text, tokens, or payment payloads.
- Production startup rejects SQLite and multiple bot replicas while local pickle FSM persistence is active.
- Compose uses PostgreSQL, readiness ordering, external secret interpolation, migrations, DB/disk health checks, and one bot replica.
- Backup, restore, retention purge, and DR documentation were added. Restore requires an explicit destructive confirmation; operational scripts support help/dry-run.
- CI now provisions PostgreSQL and uses `TEST_DATABASE_URL`.
- PostgreSQL integration coverage includes concurrent payment extension and reminder claim behavior.

Validation:
- `python -m pytest -q` — exit 0: **426 passed, 2 skipped**, 2 existing PTB warnings.
- `python -m ruff check .` — exit 0: all checks passed.
- `python -m mypy --ignore-missing-imports config.py core monitoring bot/handlers/v2 main.py` — exit 0: no issues in 31 source files.
- Earlier full `python -m mypy --ignore-missing-imports .` passed before a Windows-reserved accidental `NUL` filesystem entry disrupted recursive discovery.
- `python -m alembic heads` — exit 0: one head, `b7d8a42c9e10`.
- Backup dry-run — exit 0.
- Restore dry-run — exit 0.
- Script syntax compilation — exit 0.
- Docker Compose config with an injected validation-only password — exit 0.
- PostgreSQL tests skipped locally because `TEST_DATABASE_URL` was absent; CI is configured to run them.
- No staged files; pre-existing user changes were preserved.

Open risks/questions:
- The live PostgreSQL concurrency suite was not executed locally because no test database URL was available.
- Migration should be exercised against a staging snapshot before production rollout.
- Retention intentionally excludes financial records. Project deletion may be blocked by dependent rows and therefore requires review before `--apply`.
- A Windows-reserved untracked `NUL` filesystem artifact was accidentally created by shell redirection during validation. It is untracked and unrelated to application code, but could not be removed through the available Windows shell semantics.
- Existing PTB ConversationHandler warnings remain; the requested replica safety is enforced through singleton fail-fast rather than introducing an unreviewed shared persistence dependency.

Recommended next step:
1. Run CI or set `TEST_DATABASE_URL` against an isolated PostgreSQL database.
2. Back up staging and run `alembic upgrade head`.
3. Review paused legacy duplicate channels.
4. Exercise backup/restore in isolation.
5. Deploy with externally managed `POSTGRES_PASSWORD`, `ENVIRONMENT=production`, and `BOT_REPLICAS=1`.